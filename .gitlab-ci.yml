image: docker:stable
services:
  - docker:dind

stages:
  - build
  - test

before_script:
  - apk update && pip install docker-compose
  - pip install -r requirements.txt

Build:
  stage: build
  before_script: []
  script:
    - echo "Build time"

Test:
  stage: test
  script:
    - python3 manage.py test
